# Data Studio 依赖清单
# 安装：pip install -r requirements.txt

pandas>=2.0        # 数据操作核心
numpy>=1.24
fastapi>=0.110     # API 层
uvicorn[standard]>=0.27
openpyxl>=3.1      # Excel 读写
python-multipart   # 文件上传解析

# 可选：连数据库时才需要
sqlalchemy>=2.0
pymysql>=1.1       # MySQL
# psycopg2-binary   # PostgreSQL（如需再装）
# pyarrow           # parquet 支持（如需再装）
